defmodule OrderBook do
  @moduledoc """
  Documentation for OrderBook module.
  """

  @doc """
    OrderBook module maintains bid and ask lists as sorted trees.
  """
    
    def best_bid(book) do
        nil
    end

    def best_ask(book) do
        nil
    end

    def apply_delta(book, delta) do
        nil
    end

    def apply_snapshot(book, snapshot) do
        nil
    end

    def get_asks(book) do
        nil
    end

    def get_bids(book) do
        nil
    end

end
